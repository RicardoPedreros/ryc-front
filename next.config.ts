import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: [
  "wm9crc-ip-45-5-184-2.tunnelmole.net",
  ],
};

export default nextConfig;
